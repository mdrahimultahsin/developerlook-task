import React from 'react';

const Sticky = () => {
        return (
                <div    className='sticky top-5 w-full h-20 bg-red-500'>
                        Sticky
                </div>
        );
};

export default Sticky;